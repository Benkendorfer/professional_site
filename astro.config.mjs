// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  // Custom domain served at the root (see public/CNAME), so no `base` is needed.
  site: 'https://keesbenkendorfer.com',
});
